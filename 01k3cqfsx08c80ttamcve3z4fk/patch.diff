--- a/tools/ota_deploy.py
+++ b/tools/ota_deploy.py
@@ -97,14 +97,9 @@ class OTADeployer:
                 ['git', 'describe', '--tags', '--always', '--dirty'],
                 capture_output=True,
                 text=True,
                 check=True
             )
             version = result.stdout.strip()
-
-            # Save version for future use
-            with open(version_file, 'w') as f:
-                f.write(version)
-            self.version_file_created = True
-
             return version
         except subprocess.CalledProcessError:
             # Fallback version
@@ -408,9 +408,18 @@ class OTADeployer:
     def deploy(self, force_update: bool = False, check_only: bool = False) -> bool:
         """Complete deployment process"""
         # Get version
         version = self.get_version()
         print(f"Deploying version: {version}")
+
+        # Clean up version.txt file if it exists (patch: remove version file after deployment)
+        version_file = self.config["build"]["version_file"]
+        if os.path.exists(version_file):
+            try:
+                os.remove(version_file)
+                print(f"✓ Removed version file: {version_file}")
+            except Exception as e:
+                print(f"Warning: Could not remove version file: {e}")
+
         # Check firmware exists
         firmware_path = self.config["build"]["firmware_path"]
         if not os.path.exists(firmware_path):
             print(f"Error: Firmware not found at {firmware_path}")
